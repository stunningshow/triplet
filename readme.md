Given an array consists of non-negative integers, the task is to count the numbers of triplets chosen from the array that can make triangles if we take them as side lengths of a **triangle**.

Different Methods
